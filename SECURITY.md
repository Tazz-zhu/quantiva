# 安全说明

## 报告漏洞

请**不要**在公开 Issue 中提交安全漏洞细节。请发送邮件至项目维护者（GitHub 仓库首页可见），或使用 GitHub 私有漏洞披露功能。

## 安全基线

- 默认开启访问认证；首次登录后请立即修改默认密码 `admin123`
- 交易所 / AI / 飞书密钥通过**环境变量**注入（`CCXT_API_KEY`、`DEEPSEEK_API_KEY`、`FEISHU_WEBHOOK` 等），不写入代码与配置文件
- 配置接口对敏感字段自动脱敏，修改密码后吊销全部旧会话
- 登录接口内置失败锁定（5 次失败锁定 15 分钟）
- 实盘启动/恢复需要二次确认；提供日亏损与组合回撤双熔断

## 建议的部署安全措施

- 立即修改默认密码，并使用强密码
- 交易所 API Key 绑定服务器 IP，并仅授予所需权限（禁止提现）
- 生产环境务必启用 HTTPS（见 `deploy/nginx.conf.example` 与 `setup_https.sh`）
- 不要将 `.env`、`data/`、`config/` 中任何真实密钥提交到仓库
- 定期备份 `data/` 目录
