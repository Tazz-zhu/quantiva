# Quantiva UI 优化清单（高级 UI/前端设计师视角 · 20 条）

> 审查日期：2026-08-09 ｜ 审查范围：设计系统（色彩/字体/间距）、布局（侧栏/顶栏/内容区）、组件（卡片/表格/按钮/表单/弹窗/Toast）、动效、空态、响应式、无障碍
> 状态图例：✅ 已实施

---

## 设计系统（色彩 / 字体 / 质感）

### 1. ✅ 升级色彩令牌与质感层级
- 问题：单一平铺深色，缺少层次与品牌记忆点。
- 方案：统一色板（蓝紫主渐变）、表面分层（bg → surface → surface-2/3 带极细微透明度）、新增 glow/阴影令牌；背景加入低对比度网格 + 径向光晕营造交易终端氛围。

### 2. ✅ 排版系统精修
- 问题：数字/价格字体未统一等宽数字，标题层级弱。
- 方案：全局 font-feature-settings "tnum" 数字等宽；标题/标签统一字重与字距；统计数字用大号 tabular-nums。

### 3. ✅ 表面质感（内高光 + 玻璃拟态）
- 问题：卡片/顶栏为纯色块，缺乏精致感。
- 方案：卡片 1px 顶部内高光；顶栏/侧栏/弹窗 backdrop-filter 毛玻璃；阴影分层（hover 提升）。

### 4. ✅ 圆角与间距系统统一
- 问题：圆角 6/8px 偏小偏硬，间距不够呼吸感。
- 方案：radius 10/12px 体系；网格间距 14→16px；内容区留白加大。

---

## 布局

### 5. ✅ 侧边导航高级化
- 问题：导航项朴素，无图标容器与当前页反馈。
- 方案：图标装入圆角容器，hover 滑动高亮；激活态渐变胶囊 + 左侧指示条；底栏状态更精致。

### 6. ✅ 顶栏信息层级
- 问题：顶栏只有标题与时钟，信息密度低。
- 方案：标题区带页面图标占位；时钟等宽字体 + 日期；顶栏毛玻璃。

### 7. ✅ 登录页品牌化
- 问题：登录页仅居中卡片，缺少品牌氛围。
- 方案：渐变网格背景 + 光晕、卡片辉光边框、品牌 Logo 展示、按钮主渐变、版本标签。

---

## 组件

### 8. ✅ 卡片体系（悬浮反馈）
- 问题：hover 只有边框变色，反馈弱。
- 方案：.card.hover 悬浮上移 + 阴影增强 + 边框发光；标题图标统一主色。

### 9. ✅ 按钮体系
- 问题：主按钮纯色平铺。
- 方案：主按钮渐变 + 柔光阴影 + focus-visible 焦点环 + 按压微缩；危险/次级按钮统一规范。

### 10. ✅ 表单控件
- 问题：输入框无内阴影、数字输入框带原生箭头。
- 方案：输入框内阴影 + focus 光晕；隐藏 number 上下箭头；下拉箭头重绘；标签字距统一。

### 11. ✅ 表格
- 问题：表头/行 hover 平淡，数字列无对齐层次。
- 方案：表头渐变底色 + 吸顶；行 hover 左侧指示条 + 高亮；数字列 tabular-nums；容器圆角统一。

### 12. ✅ 徽章 / 标签
- 问题：徽章方角、颜色单一。
- 方案：徽章改胶囊形 + 柔和底纹；chips 激活态渐变 + 阴影。

### 13. ✅ Toast 通知
- 问题：通知扁平、无类型辨识度。
- 方案：毛玻璃 + 左侧类型色条；进入/退出动画更顺滑；阴影加深。

### 14. ✅ 弹窗（Modal）
- 问题：无入场动画、遮罩纯黑。
- 方案：遮罩毛玻璃 + 淡入；弹窗 scale+fade 入场；危险弹窗红晕边框。

### 15. ✅ 空态 / 加载态
- 问题：空态图标为虚线框，加载只有小转圈。
- 方案：空态图标渐变描边 + 微光；spinner 更精致；卡片骨架屏（CSS 就绪）。

### 16. ✅ 滚动条
- 问题：滚动条样式朴素。
- 方案：更细滚动条 + 圆角渐变滑块，hover 增亮。

---

## 动效 / 无障碍 / 响应式

### 17. ✅ 页面切换与微交互
- 问题：页面切换无过渡。
- 方案：页面淡入上移动画；卡片/按钮/导航统一 150-200ms 过渡曲线。

### 18. ✅ 焦点可见性与无障碍
- 问题：键盘 Tab 焦点不明显。
- 方案：全局 :focus-visible 高亮环；链接/按钮 hover 状态增强。

### 19. ✅ 响应式增强
- 问题：仅 1100px 一档断点，窄屏侧栏占宽。
- 方案：新增 900px / 720px 断点：侧栏收窄为图标栏、网格降列、表格横向滚动。

### 20. ✅ 数据可视化细节
- 问题：图表工具条/标题与卡片体系割裂。
- 方案：图表工具条圆角化、标题字距统一、十字光标浮层投影统一、小图高度规范。

---

## 附：实施汇总
| 类别 | 数量 |
| --- | --- |
| 设计系统 | 4（#1-#4） |
| 布局 | 3（#5-#7） |
| 组件 | 9（#8-#16） |
| 动效/无障碍/响应式 | 4（#17-#20） |

> 设计原则：克制的深色 + 品牌渐变点缀 + 克制的动效；所有改动兼容离线运行（不依赖外网字体/CDN）。
