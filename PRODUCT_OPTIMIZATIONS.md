# Quantiva 产品优化清单（产品经理视角 · 20 条）

> 审查日期：2026-08-09
> 审查范围：Web 控制台（9 个页面）、后端 API、回测引擎、实盘/模拟盘、监控、进化、数据、系统设置
> 状态图例：✅ 已实施

---

## P0 · 必须修复（影响核心功能 / 资金安全）

### 1. ✅ 设置页「保存全部配置」失效
- 问题：settings.js 调用 POST /api/config，但后端没有该路由，保存必失败（405）。
- 方案：新增 POST /api/config，合并默认值、脱敏字段回写保护、保存后热重载服务并写入审计。
- 验证：保存后配置立即生效，审计日志出现「保存配置」。

### 2. ✅ 历史数据抓取不完整（无分页）
- 问题：ExchangeDataFetcher.fetch_ohlcv 单次调用 ccxt，交易所默认只返回约 1000 根 K 线；「数据管理」宣称抓 730 天 1h（约 17520 根）实际只落库 1000 根。
- 方案：实现分页抓取（按交易所单页上限循环翻页直到目标天数），供数据管理、图表、回测共用。
- 验证：抓 365 天 1h 落库数量 ≈ 8760 根。

### 3. ✅ 实盘启动缺少二次确认与风险提示
- 问题：实盘模式（真金白银）点击启动即下单，无任何确认；误触风险极高。
- 方案：前端实盘启动弹强确认弹窗（红色警示 + 输入 CONFIRM），后端要求 confirm_live: true 才允许实盘启动；配置项 risk.require_live_confirm 可关闭。
- 验证：未确认时启动被后端拒绝（400）。

---

## P1 · 重要优化（风控 / 安全 / 数据可用性）

### 4. ✅ 日亏损熔断未实现（配置预留字段形同虚设）
- 问题：risk.max_daily_loss_pct 在配置里标注「预留」，实盘循环完全没有使用；单日连续亏损会无限放大。
- 方案：实盘/模拟盘循环实现日亏损熔断——按 UTC 日统计已实现盈亏，达到阈值后平仓并禁止新开仓，次日自动复位；状态接口与前端暴露熔断信息。
- 验证：设置 2% 熔断，连续亏损后可观察到「日亏损熔断中」且不再开新仓。

### 5. ✅ 登录无暴力破解防护
- 问题：登录接口无失败次数限制，可被脚本爆破。
- 方案：按「用户名+IP」记录失败次数，5 次失败锁定 15 分钟，成功登录清零；审计记录锁定事件。
- 验证：连续输错 5 次后返回「尝试次数过多，请稍后再试」。

### 6. ✅ 修改密码后旧 Token 仍然有效
- 问题：改密只替换哈希，已签发 Token 全部继续有效，存在会话残留风险。
- 方案：改密成功后吊销全部既有 Token，要求重新登录。
- 验证：改密后旧 Token 请求返回 401。

### 7. ✅ 默认密码 admin123 缺少显著提醒
- 问题：登录后仅登录页提示一次，日常使用无感知；长期不修改风险高。
- 方案：全局顶部常驻黄色警示条「当前使用默认密码，请立即修改」+ 一键跳转设置页；修改后自动消失。
- 验证：默认密码登录后顶部出现警示条。

### 8. ✅ 前端大量静默吞错，故障不可见
- 问题：仪表盘/实盘/监控/进化等 refresh() 的 catch 为空，接口失败时页面显示「—」或旧数据，用户误以为正常。
- 方案：统一错误提示（页面状态条 + 事件列表），区分「数据源离线」与「接口错误」。
- 验证：停掉后端后刷新页面能看到明确错误提示。

### 9. ✅ 数据管理缺少删除/清理能力
- 问题：本地数据库只能抓、不能删，长期积累脏数据无法清理。
- 方案：新增删除指定标的+周期的接口与 UI（二次确认 + 审计）。
- 验证：删除后统计列表消失，审计有记录。

### 10. ✅ 监控事件无法筛选/分页（历史事件难回溯）
- 问题：/api/monitor/events 只有 limit/offset，前端只显示 100 条，无法按类型/币种筛选。
- 方案：后端支持 type 过滤 + 前端类型筛选器 + 「加载更多」。
- 验证：按「放量异动」筛选只显示对应事件。

### 11. ✅ 回测/进化交易明细无法导出 CSV
- 问题：只有 HTML 报告，无法把交易明细导入 Excel/其他分析工具。
- 方案：新增回测交易明细 CSV 与进化交易 CSV 导出（UTF-8 BOM，Excel 可直接打开）。
- 验证：点击导出得到可打开的 CSV。

### 12. ✅ 回测未计入永续合约资金费率（已知限制）
- 问题：README 明确「回测未计永续合约资金费率」，导致趋势持仓收益被高估。
- 方案：回测参数新增「8 小时资金费率」，引擎按持仓每根 K 线扣费；前端回测参数可配置；README 更新。
- 验证：资金费率 0.1% 时长期持仓回测收益显著下降，指标含 funding_paid。

---

## P2 · 体验优化（易用性 / 信息密度 / 文档）

### 13. ✅ 仪表盘缺买入持有基准与更多关键信息
- 问题：KPI 只显示回测收益，没有与买入持有对比的语境；实盘运行时长、熔断状态等未展示。
- 方案：回测 KPI 增加「买入持有」对照；实盘状态卡片展示熔断状态。
- 验证：仪表盘能看到「最新回测 vs 买入持有」。

### 14. ✅ 缺少新手引导（功能多、上手成本高）
- 问题：9 个页面功能丰富，但首次使用无指引。
- 方案：仪表盘增加可关闭的「快速上手」引导卡（推荐路径：看行情 → 跑回测 → 参数搜索 → 模拟盘 → 实盘），含跳转按钮。
- 验证：首次登录可见引导卡，可关闭。

### 15. ✅ 图表缺少 OHLC 数值读取（十字光标）
- 问题：自绘 SVG 图表只有缩放/平移，鼠标悬停看不到具体 OHLC，专业用户无法精读。
- 方案：K 线图悬停显示竖线 + OHLC/涨跌幅浮层。
- 验证：鼠标移到 K 线上显示 OHLC 数值。

### 16. ✅ 版本信息与更新日志缺失
- 问题：界面固定显示 v1.0，用户无法感知更新。
- 方案：后端版本常量、/api/health 返回版本；界面显示 v1.1；README 增加更新日志。
- 验证：健康检查返回版本号，界面显示 v1.1。

### 17. ✅ README 与实现不一致
- 问题：README 的「已知限制」部分（无资金费率等）与实际能力/本次修复不一致。
- 方案：同步更新 README（功能清单、已知限制、配置说明、更新日志）。
- 验证：文档与实际行为一致。

### 18. ✅ 实时推送（SSE）替代轮询
- 问题：实盘/监控靠 4 秒轮询，网络开销大、延迟不稳定。
- 方案：新增 /api/stream（SSE）推送实盘/监控状态（约 2s），前端 EventSource 订阅、断线 3 次自动回退轮询；仅当前页建立连接，切页自动关闭。
- 验证：实盘/监控页打开后可见状态自动刷新，断网后回退轮询仍可工作。

### 19. ✅ 多策略同图对比模式
- 问题：只能单策略回测，无法直观对比多个策略在同一行情下的表现。
- 方案：新增 /api/backtest/compare 并行回测任务；回测页「多策略同图对比」面板支持 2-4 个策略（参数 JSON 可自定义），结果以权益曲线同图 + 指标对比表展示。
- 验证：选择双均线 / RSI 两个策略对比，同图曲线与指标表正确展示。

### 20. ✅ 实盘会话持久化与重启恢复
- 问题：服务重启后实盘循环停止且参数丢失（仅内存），无「恢复上次会话」能力。
- 方案：启动时自动保存会话到 data/live_session.json，停止时清除；新增 /api/live/session、/api/live/recover、DELETE /api/live/session；实盘页展示「上次会话」卡片，一键恢复或清除，实盘恢复需二次确认。
- 验证：启动模拟盘 → 停止后页面仍显示会话卡 → 一键恢复成功；实盘模式恢复需输入 CONFIRM。

---

## 附：本次实施汇总

| 类别 | 数量 | 明细 |
| --- | --- | --- |
| 已实施 | 20 | #1-#20 |
| 已排期建议 | 0 | - |
| 后端改动 | - | fetcher / server / auth / live_manager / engine / state / monitor |
| 前端改动 | - | app / main / charts / 9 个页面 / index.html / style.css |
| 文档 | - | README、本清单 |

> 建议后续迭代方向：多标的组合投资组合、策略自动部署闭环、PostgreSQL 高并发支持；实盘资金相关改动必须先在模拟盘充分验证。
